 <!DOCTYPE html>
 <html>
 <head>
    <title>index</title>
    </head>
    <style type="text/css">
    *{margin: 0;padding: 0;list-style: none}
    .New_List{width: 500px;height: 100px;overflow: hidden;margin: 0 auto}
    .New_List li{width: 500px;height: 30px;line-height: 30px;overflow: hidden;}
    .New_List li a{float: left;}
    .New_List li span{float: right;}
    </style>
    <body>
        <div class='New_List'>
            <h3>News List</h3>
                <ul>
                    {% for item in news_list %}
                   <li><a href="/files/helloshiyanlou">{{ item.title }}</a><span>{{ item.created_time }}</span></li>
                    {% endfor %}
               <ul>
    </body>
</html>
